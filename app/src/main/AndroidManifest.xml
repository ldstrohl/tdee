<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- WorkerFoodParser: POST meal text to the Cloudflare /parse endpoint. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Health Connect: read body weight. -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />

    <!-- Lets us detect / launch the Health Connect app (alpha11 routes through it). -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".TdeeApplication"
        android:label="TDEE"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Permissions-rationale screen required by Health Connect.

            API < 34 path (our target Pixel 3 / API 31): Health Connect launches the
            activity that declares the ACTION_SHOW_PERMISSIONS_RATIONALE intent filter
            when the user taps "privacy policy" in the HC permission sheet.
        -->
        <activity
            android:name=".health.PermissionsRationaleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!--
            API 34+ path: Health Connect is part of the platform and surfaces the
            rationale via the standard VIEW_PERMISSION_USAGE intent against an
            activity-alias whose health_permissions meta-data lists our permissions.
            Not exercised on API 31 but declared for forward compatibility.
        -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".health.PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
            <meta-data
                android:name="health_permissions"
                android:resource="@array/health_permissions" />
        </activity-alias>
        <!-- Module 7: share exported CSV from cacheDir/export via a content URI. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
